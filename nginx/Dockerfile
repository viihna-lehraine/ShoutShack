# Use the official Nginx image from the Docker Hub
FROM nginx:alpine

COPY nginx.conf /etc/nginx/nginx.conf

# Docker Metadata
LABEL maintainer="Viihna Lehraine"
LABEL version="1.0"
LABEL description="Nginx reverse proxy for Guestbook project"